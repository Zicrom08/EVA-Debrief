{
  "name": "eva-debrief-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Frontend SPA de EVA Debrief (Vite, aucun framework UI).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test test/"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  }
}
